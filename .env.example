# Volcano Ark (Doubao). Replace ARK_API_KEY with a real key.
ARK_BASE_URL=https://ark.cn-beijing.volces.com
ARK_API_KEY=replace-with-real-key
ARK_MODEL=doubao-seed-2-0-mini-260215

# Optional for --transport http. If set, clients must send:
# Authorization: Bearer <VISION_MCP_TOKEN>
VISION_MCP_TOKEN=
